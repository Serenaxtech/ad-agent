from setuptools import setup, find_packages

setup(
    name="configreader",
    version="0.1",
    packages=find_packages(),
    description="A Config Reader Package",
    author="Example",
    install_requires=[],
)